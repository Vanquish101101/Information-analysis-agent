# 9. Задачник — Information Analysis Agent (Агент 3)

Чек-лист выполненных и оставшихся этапов работы, по аналогии с `06. Задачник.md` Агента 1.
Разворачивается по мере работы — каждая выполненная задача отмечается здесь.
Развёрнутое описание каждого этапа (что именно делалось) — в `Журнал разработки.md`.

---

## Слайс 1 — Ingestion-слой (v0.1.0)

- [x] Скаффолдинг проекта (`package.json`, `node:test`)
- [x] Supabase client factory
- [x] Схема Supabase (`entities`/`claims`/`sources`/`runs`/`pending_user_decisions`), применена к живой БД
- [x] Фейковый Supabase query builder для тестов
- [x] `normalizeItem` — единый формат входа от Агента 1/2
- [x] Ридер `intelligence_agent.search_results` (Агент 1)
- [x] Ридер `agent3_handoff_queue` + join `parsing_results`/`parsing_jobs` (Агент 2)

## Слайс 2 — Планировщик батча (v0.1.0 → v0.2.0)

- [x] `decideAction` — логика тишины 15 мин / потолка 11:00
- [x] `pollQueues` — объединение Агент 1/Агент 2 с фильтром по курсору
- [x] `InMemoryStateStore`
- [x] `createScheduler` — `checkOnce`/`start`/`stop`, колбэк `onBatchReady`
- [x] Fix: сброс триггера по UTC-дате (не по булеву флагу)

## Слайс 3 — Граф анализа LangGraph, Шаг 4 (v0.2.0 → v0.3.0)

- [x] `extractClaims` — извлечение claims через OpenRouter (реальный вызов)
- [x] `AnalysisState` + зависимости LangGraph
- [x] `dispatchToExtraction` — Send fan-out + обработка пустого батча
- [x] Узел извлечения claims в графе — источник + изоляция ошибок
- [x] `reducer` — фиксация агрегатов
- [x] `persistResults` — запись `runs`/`sources`/`entities`/`claims` (без дедупа)
- [x] Сборка графа `createAnalysisGraph`/`runAnalysis`
- [x] Проверено реальным сквозным прогоном (OpenRouter + живая Supabase)

## Слайс 4 — Точка входа + Redis + Docker-деплой (v0.3.0 → v0.4.0)

- [x] `RedisStateStore` — состояние планировщика в общем Redis
- [x] Fix: `checkOnce` теперь await-ит `StateStore` (нужно для Redis)
- [x] `src/index.js` — точка входа, планировщик подключён к графу
- [x] Dockerfile + docker-compose.yml, задеплоено на `marketing-agency-net`
- [x] Fix: Redis-healthcheck при старте + расширение try/catch в `checkOnce`
- [x] Проверено реальным запуском контейнера рядом с Агентом 1/2

## Слайс 5 — Дедупликация, Шаг 5 (v0.4.0, в работе)

- [x] Миграция `002_dedup_match_functions.sql` — SQL-функции векторного поиска (применена к живой БД)
- [x] `embedText` — эмбеддинги через Gemini (768 измерений, подтверждено живым вызовом)
- [x] `judgeDuplicate` — LLM-judge для подтверждения дублей
- [x] `dedup`-узел — резолв entity/claim через embedding + LLM-judge
- [x] Обновление `persistResults.js` — резолв entity/дублей от dedup, запись embedding
- [x] Подключение `dedup` в граф между `reducer` и `persistResults`
- [x] Финальный ревью слайса (чисто, 119 тестов проходят)
- [x] Применение миграции к живой БД + реальный сквозной прогон (подтверждён живой дедуп entity и claim через embedding + LLM-judge)
- [x] Fix: `src/index.js` не передавал `embedText`/`judgeDuplicate` в `createAnalysisGraph` — точка входа падала бы при старте (найдено при подготовке сквозного прогона, не покрыто финальным ревью т.к. вне диапазона диффа плана)

## Наблюдаемость — LangSmith + Helicone (ТЗ §5, нефункциональные требования)

- [x] LangSmith — трейсинг графа (`LANGCHAIN_TRACING_V2`/`LANGCHAIN_API_KEY`/`LANGCHAIN_PROJECT`, автоинструментирование через `@langchain/core`, без изменений кода). Fix: изначально использованный "Service API" ключ давал 403 при реальной отправке трейсов (только чтение работало) — заменён на "Personal API" ключ, подтверждено реальной отправкой без ошибок
- [x] Helicone — прокси-логирование LLM-вызовов (`extractClaims`/`judgeDuplicate` через `openrouter.helicone.ai`, `embedText` через `gateway.helicone.ai`), опционально через `heliconeApiKey`, подтверждено живыми вызовами
- [ ] Настройка Helicone/LangSmith для Агента 1/Агента 2 (общая инфра, отложено — сделать для всех агентов разом, по решению пользователя)

## Слайс 6 — Детекция противоречий, Шаг 6

- [x] Миграция `003_contradictions.sql` — таблица противоречий (применена к живой БД)
- [x] `judgeContradiction` — LLM-judge {agree|contradict|unclear} для детекции противоречий
- [x] `dedup.js` — прокидывает отклонённого кандидата как `contradictionCandidate`
- [x] `contradiction`-узел — self-consistency (3 сэмпла) для противоречий с claim высокой уверенности
- [x] `persistResults.js` — запись `contradictions` после вставки claim
- [x] Подключение `contradiction` в граф между `dedup` и `persistResults` + `src/index.js`
- [x] Финальный ревью слайса (1 Minor дефект найден и исправлен: confidence/explanation теперь берутся из вердикта, совпадающего с итоговой меткой; 156 тестов проходят)
- [x] Применение миграции к живой БД + реальный сквозной прогон (подтверждена реальная детекция противоречия 5 млн vs 50 млн, self-consistency, запись в contradictions)

## Слайс 7 — Эскалация / контроль стоимости, Шаг 7 (v0.4.0)

- [x] Дизайн-спека (`docs/superpowers/specs/2026-07-08-escalation-cost-control-design.md`)
- [x] План реализации, 15 задач (`docs/superpowers/plans/2026-07-08-escalation-cost-control.md`)
- [x] Миграция `004_cost_columns.sql` (`runs.cost_usd_retry`/`cost_usd_analysis`)
- [x] `content_ref` прокинут через ингест (Агент 2 → нормализация)
- [x] Реальный `costUsd` в `extractClaims.js`/`judgeDuplicate.js`/`judgeContradiction.js` (OpenRouter `usage:{include:true}`)
- [x] Оценка `costUsd` в `embedText.js` (Gemini не отдаёт реальную стоимость)
- [x] `AnalysisState` — новые каналы (`costUsdAnalysis` sum-reducer, `costUsdRetry`/`escalationsAuto`/`escalationsPendingUser`/`costCapReached`)
- [x] Узлы `extractClaims`/`dedup`/`contradiction` суммируют стоимость в `costUsdAnalysis`
- [x] `deepParsingClient.js` — MCP-клиент к Агенту 2 (`@modelcontextprotocol/sdk`, Streamable HTTP, `mode: 'deep'`)
- [x] Узел `escalation` — автоповтор/пороги/cost cap $5, эскалация в `pending_user_decisions`
- [x] `persistResults.js` пишет `cost_usd`/`cost_usd_analysis`/`cost_usd_retry`/счётчики эскалаций, статус `cost_cap_reached`
- [x] `escalation` вшит первым узлом графа, `src/index.js` подключает `retryParse`
- [x] Финальный ревью слайса (1 Critical + 1 Important + 2 Minor дефекта найдены и исправлены: `deepParsingClient.js` слепо принимал асинхронный `{status:'queued'}` ответ Агента 2 как успех — теперь явно проверяет форму ответа; потеря уже потраченной стоимости при частичном сбое цепочки вызовов — исправлено через costTracker-аккумулятор; неизвестный `content_type` больше не считается "дешёвым" по умолчанию; MCP-клиент закрывается даже при сбое `connect()`. 200 тестов проходят)
- [x] Применение миграции к живой БД + реальный сквозной прогон (Docker Desktop + живые контейнеры Агента 2 подняты; подтверждены ОБА сценария Critical-фикса на реальной системе: (1) свежий низкоуверенный item с `mode:'deep'` без кэша получил от Агента 2 настоящий асинхронный `{status:'queued'}`, наш код корректно отверг это и эскалировал вместо ложного успеха; (2) повторный вызов того же `content_ref` после того, как фоновый воркер Агента 2 реально досчитал задачу (подтверждено логами контейнера), попал в кэш Агента 2 и получил настоящий синхронный результат — корректно принят как успешный автоповтор с реальной стоимостью $0.0015 в `cost_usd_retry`. Учёт стоимости для высокоуверенного item'а тоже подтверждён вживую. Все тестовые данные вычищены из БД)

## Слайс 8 — GlobalSynthesis, Шаг 7-8 (v0.5.0)

- [x] Дизайн-спека (`docs/superpowers/specs/2026-07-09-global-synthesis-design.md`)
- [x] План реализации, 9 задач (`docs/superpowers/plans/2026-07-09-global-synthesis.md`), выполнен через реальных субагентов (subagent-driven-development)
- [x] Миграция `005_digest.sql` (`claim_sources` junction, `sources.reach_estimate`, `digests`, RPC `claim_source_stats`)
- [x] `AnalysisState` — каналы `persistedFacts`/`persistedContradictions`
- [x] `normalize.js` — best-effort `reachEstimate` (сумма views+likes из YouTube-результатов Агента 1, 0 для остального)
- [x] Узел `extractClaims` прокидывает `reachEstimate` в `claim.source`
- [x] `persistResults.js` пишет `claim_sources` (и для новых claim'ов, и для подтверждённых дублей — закрыт реальный пробел с честным `sources_count`), `sources.reach_estimate`, возвращает `persistedFacts`/`persistedContradictions`
- [x] `src/llm/globalSynthesis.js` — `synthesizeDigest` через `claude-sonnet-4-6` (первая не-haiku модель в проекте)
- [x] Узел `globalSynthesis` — последний в графе, собирает `facts[]`/`contradictions[]`/`meta` (формат `analysis_digest` из ТЗ §3.2), сохраняет в `digests`
- [x] `graph/index.js` и `src/index.js` подключают `synthesizeDigest` (grep-проверка)
- [x] Финальный ревью слайса (1 Important дефект найден и исправлен: узел globalSynthesis терял реально потраченную стоимость LLM-синтеза при сбое парсинга ответа модели или при сбое сохранения дайджеста — теперь стоимость докручивается в `runs` на всех путях. 241 тест проходит)
- [x] Применение миграции к живой БД + реальный сквозной прогон (найден и исправлен реальный баг: миграция 005 не давала GRANT на новые таблицы claim_sources/digests сервисной роли — "permission denied", RPC-функция грант получила, таблицы нет; исправлено + применено к живой БД. После фикса подтверждён честный сквозной прогон: реальный digest сохранён, sources_count и reach_estimate — настоящие числа (12800 = 12000 просмотров + 800 лайков YouTube-результата), не заглушки. Все тестовые данные вычищены)

## Слайс 9 — MCP-сервер (выход) (v0.5.0)

- [x] Дизайн-спека (`docs/superpowers/specs/2026-07-09-mcp-server-design.md`)
- [x] План реализации, 5 задач (`docs/superpowers/plans/2026-07-09-mcp-server.md`), выполнен через реальных субагентов
- [x] `mcp-server/queries.js` — `getDigest`/`getClaimDetail`/`getStatus` напрямую к БД (не через JSONB-поиск)
- [x] `mcp-server/server.js` — `listTools`/`callTool`/`createMcpServer`, три read-only инструмента (тот же паттерн, что у Агента 2)
- [x] `mcp-server/http.js` — Streamable HTTP транспорт (тот же обход бага Node v24 в `@hono/node-server`, что у Агента 2), фабрика, не самозапускающийся скрипт
- [x] `src/index.js` запускает MCP-сервер рядом с планировщиком (порт 7302, grep-проверка)
- [x] Docker: порт 7302 у Агента 3, подключение к общей инфраструктуре (мастер-compose)
- [x] Финальный ревью слайса (1 Important дефект найден и исправлен: маршрут `/mcp` — самый новый и рискованный код слайса — не был покрыт тестами вообще; добавлены 3 реальных сквозных теста через настоящий MCP-клиент. 269 тестов проходит)
- [ ] Живой сквозной прогон в Docker — ожидает подтверждения пользователя
- [ ] Раскомментированная строка Агента 3 в мастер `Инфраструктура (Docker)/docker-compose.yml` реально отредактирована на диске, но не закоммичена — этот файл принадлежит отдельному родительскому репозиторию с большим количеством несвязанных незакоммиченных изменений, решение о коммите там — за пользователем

## Слайс 10 — Telegram-уведомления

- [ ] Не начато

## v1.5 / v2.0 (после MVP)

- [ ] Персистентность графа (LangGraph checkpoints)
- [ ] Точная калибровка порога similarity дедупликации (сейчас 0.85)
- [ ] Дашборд/отчёт по расходам за прогон
- [ ] Алгоритм рейтинга источников
- [ ] Алгоритм отслеживания нарратива во времени
- [ ] Адаптивная частота анализа
