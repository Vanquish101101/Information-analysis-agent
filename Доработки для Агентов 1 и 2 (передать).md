# Доработки для Агентов 1 и 2 (передать)

> Написано 2026-07-09 по завершении Агента 3 (Information Analysis Agent). Агент 3 полностью
> реализован по ТЗ (все слайсы 1–10, см. `План разработки/9. Задачник.md`) и не будет дальше
> развиваться в этой сессии — по правилу разделения зон ответственности между агентами. Этот файл —
> ровно то, что нужно доделать в Агенте 1 и Агенте 2, чтобы весь конвейер из 4 агентов реально
> заработал end-to-end, а не просто «готов на бумаге». Все пункты ниже проверены чтением
> исходного кода соответствующих проектов и живым запросом к продовой Supabase — не догадки.

---

## ✅ Исправлено (2026-07-09) — конвейер не читал реальные данные Агента 1 и Агента 2

Это была не задача для Агента 1 или Агента 2 — баг был в коде **самого Агента 3**
(`Information analysis agent/Code/src/ingestion/agent1Reader.js` и `agent2Reader.js`). Оставляю
описание здесь первым пунктом для истории и потому что он объясняет и обесценивает часть того,
что раньше казалось уже проверенным (см. коммиты `304577c` — сам фикс, и `b5126f5` — сопутствующий
редеплой контейнера на `v0.6.0`). **Статус: исправлено, покрыто regression-тестами, подтверждено
живым запросом к проду.**

**Что обнаружено.** Supabase-клиент Агента 3 создаётся с фиксированной схемой:

```js
// Code/src/db/client.js
createClient(url, serviceKey, { db: { schema: 'information_analysis_agent' } })
```

Но `search_results` физически живёт в схеме `intelligence_agent` (Агент 1), а
`agent3_handoff_queue`/`parsing_results`/`parsing_jobs` — в схеме `deep_parsing_agent` (Агент 2).
Код читает их как `db.from('search_results')` / `db.from('agent3_handoff_queue')` — **без**
`.schema('intelligence_agent')` / `.schema('deep_parsing_agent')`. Я проверил это вживую прямо
сейчас реальным запросом через настоящий `@supabase/supabase-js` клиент с реальными продовыми
ключами:

- `db.from('search_results')` → `404 PGRST205: Could not find the table
  'information_analysis_agent.search_results' in the schema cache`
- `db.from('agent3_handoff_queue')` → та же ошибка, другая таблица
- `db.schema('intelligence_agent').from('search_results')` → **успех**, вернул реальную живую
  запись (прогон Агента 1 от 2026-07-07)
- `db.schema('deep_parsing_agent').from('agent3_handoff_queue')` → **успех**, вернул реальную
  запись (в очереди сейчас 13 pending job'ов от Агента 2, ждут Агента 3)

**Что это значит на практике.** `pollQueues.js` вызывает эти два ридера через `Promise.allSettled`
и при ошибке просто пишет `console.error` и продолжает с пустым результатом (специально, чтобы
сбой одного источника не ронял другой). Из-за этого **каждый плановый прогон Агента 3 молча
получает 0 новых элементов от Агента 1 и Агента 2**, даже когда данные реально есть (сейчас в базе
лежит минимум 1 необработанная запись Агента 1 и 13 — Агента 2). Ни одна ошибка при этом не всплывает
пользователю — просто "нечего анализировать". Это объясняет фразу в `Deep parsing agent/10.
Задачник.md`: «реального сквозного вызова ещё не было» — с точки зрения планового опроса очереди
это действительно так, несмотря на то что прямые вызовы (MCP-эскалация в Слайсе 7, живые
сквозные прогоны Слайсов 3/6/8) заводили тестовые данные другим путём и поэтому этой ошибки не
видели.

**Что сделано.** Добавлен `.schema('intelligence_agent')` в `agent1Reader.js` и
`.schema('deep_parsing_agent')` во все три запроса `agent2Reader.js`. При живой проверке фикса
вскрылся второй, ранее замаскированный первым баг: `agent2Reader.js` запрашивал
`agent3_handoff_queue.created_at`, а такой колонки в реальной таблице нет (проверено live-схемой:
`id/job_id/result_ref/attempt_count/last_attempt_at/status`) — `created_at` теперь берётся из
`parsing_jobs`, где он реально есть. `fakeSupabase.js` получил `db.schema(name)`, добавлены
regression-тесты, проверяющие `db.schemaCalls`. Живая проверка после фикса: `fetchAgent1Items`
вернул 1 реальный элемент, `fetchAgent2Items` — 5 (limit тестового вызова, в очереди 13),
`pollQueues` — 14 объединённых элементов с корректным `newestSeenAt`. 294/294 теста проходят.

---

## ✅ Исправлено (2026-07-09) — RLS был отключён на 12 таблицах

Побочный результат той же проверки — Supabase-advisor сообщил, что у 12 таблиц из 3 схем
(`information_analysis_agent`, `intelligence_agent`, `deep_parsing_agent`) был отключён Row Level
Security, то есть все они были полностью открыты на чтение/запись любому, у кого есть `anon`-ключ
проекта. Не специфично для Агента 1/2 — касалось всех трёх агентов сразу.

**Применено.** Проверил `.env` всех трёх проектов — везде используется только
`SUPABASE_SERVICE_KEY`, ни один агент не подключается через `anon`-ключ. `service_role` в Supabase
имеет флаг `BYPASSRLS` и полностью игнорирует RLS независимо от наличия policy — включение RLS
было безопасно для всех троих. Применено через `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` на всех
12 таблицах. Живая проверка после применения: `pollQueues` (Агент 3) снова вернул те же 14
элементов, прямое чтение `runs` тоже отработало без ошибок — доступ `service_role` не пострадал.
Security-advisor подтвердил: ERROR-уровня findings по RLS не осталось (только безобидные INFO
«RLS включён, но нет policy», ожидаемо при доступе исключительно через `service_role`).

---

## Раздел A — Агент 1 (Intelligence agent)

Ниже — только то, что **ещё не отмечено `[x]`** в `Intelligence agent/План разработки/06.
Задачник.md` и напрямую влияет на связку с Агентом 3. Не дублирую их собственный трекер — только
привязываю к контексту полного конвейера.

1. **✅ Реализовано (2026-07-09) — Маршрутизация «сложного» контента к Агенту 2.** Новый модуль
   `Code/src/agents/router/agent2Queue.js` добавлен в Intelligence agent: `routeToAgent2(items)`
   пушит jobs в `queue:deep-parsing:jobs` (Redis RPUSH), `buildAgent2Jobs(scoutResult, topics)`
   собирает YouTube URLs → `content_type: 'video'`, тонкие страницы (<100 символов) →
   `content_type: 'text'`. Оркестратор (`Code/src/orchestrator/index.js`) вызывает routing
   fire-and-forget после каждого scout. Job-формат: `{ job_id (UUID), content_ref, content_type,
   source: 'agent1', source_query, created_at }` — совместим с консьюмером Агента 2
   (`dequeueHeavyJob()`). Также реализована подписка на `notifications:agent1` (Redis pub/sub) —
   бот получает уведомления от Агента 2 о деградированной доставке. Коммиты `13392da` + `503baff`
   в репозитории `Intelligence agent`.

2. **Ручное тестирование записи в `search_results`** (их `06. Задачник.md`, раздел 1.11
   «Тестирование», 4 пункта не отмечены). Сама запись в код добавлена и подтверждена чтением
   исходников (см. уже существующий файл `Доработки для Агента 1 (передать).md` в этой же папке —
   статус «Реализовано»), но живого теста именно записи в БД со стороны Агента 1 не было.
   Раньше это было бы бессмысленно тестировать — Агент 3 не мог прочитать `search_results` из-за
   бага со схемой (см. пункт выше, уже исправлено 2026-07-09) — теперь путь открыт, можно
   тестировать по-настоящему.

3. **LangSmith/Helicone для Агента 1** (их раздел 1.4.1, помечено 🟡 важно). У Агента 3 оба уже
   подключены и проверены живыми вызовами (см. `9. Задачник.md` этого проекта, раздел
   «Наблюдаемость»); решение о том, чтобы настраивать одинаково для всех агентов разом, уже
   принято раньше (см. память `feedback_language_russian`-соседний пункт, у Агента 3 отмечено как
   «отложено — сделать для всех агентов разом»). Когда дойдёте — можно напрямую скопировать
   рабочую конфигурацию из `Code/.env.example` Агента 3 (`LANGCHAIN_TRACING_V2`,
   `LANGCHAIN_API_KEY` — использовать **Personal API key**, не Service API key: у Агента 3 Service
   key давал 403 при реальной отправке трейсов, только чтение работало).

4. **✅ Исправлено (2026-07-09) — «Режим «Создание контента» (`mode_content`) не сохранял
   состояние».** В отличие от `mode_info` (вызывает `updateSetting(ctx.from.id, 'mode', 'info')`),
   обработчик `mode_content` не вызывал `updateSetting(..., 'content')` вообще, хотя команда
   `/mode` уже была готова отображать этот режим — просто выбрать его через кнопку было
   невозможно, состояние не сохранялось. Добавлен недостающий вызов, зеркально `mode_info`
   (коммит `42fdc28` в репозитории `Intelligence agent`). Сама реальная маршрутизация через 4
   агента в реальном времени по-прежнему не входит в объём — это отдельное, осознанно отложенное
   архитектурное видение (память Claude `project_future_unified_bot_vision`), фикс только про то,
   что кнопка теперь честно сохраняет выбор.

5. **✅ Исправлено (2026-07-09) — уведомления от Агента 2 не доходили до пользователя.**
   Обработчик Redis pub/sub `notifications:agent1` (события о деградированной доставке Агенту 3 —
   например, handoff ушёл в буфер вместо прямой доставки) только писал в лог-файл
   (`telegram-bot/index.js`) и никогда не пересылал уведомление в Telegram. При фиксе нашёлся ещё
   один, более глубокий баг в том же месте: код читал `event.type`, хотя Агент 2 реально публикует
   поле `event.event` (`Deep parsing agent/Code/src/queue/index.js`, `notifyAgent1`) — тип события
   в логах всегда был `type=?`, даже сам лог был бесполезным, не только пересылка отсутствовала.
   Исправлено: читаем правильное поле, пересылаем пользователю через
   `bot.telegram.sendMessage(ALLOWED_USER_ID, ...)` с job_id и причиной, plain text (без Markdown —
   текст ошибки произвольный, безопаснее не рисковать parse entities). Ошибка самой отправки
   ловится и логируется отдельно, не рвёт обработку канала (коммит `42fdc28`).
   **Не проверено живьём** — у Агента 1 нет тестового сьюта (только `node --check` на синтаксис),
   рекомендуется вручную перезапустить бота и спровоцировать реальное уведомление от Агента 2 после
   деплоя.

## Раздел B — Агент 2 (Deep parsing agent)

1. **Обновить `10. Задачник.md`, строку в разделе «Деплой»**: «Агент 3 не задеплоен (сам Агент 3
   ещё не задеплоен)» и «Агент 3 не задеплоен — сетевой путь и буферизация готовы, но реального
   сквозного вызова ещё не было» — оба утверждения устарели. Агент 3 задеплоен в Docker с версии
   v0.3.0 (Слайс 4), а реальный сквозной вызов MCP-эскалации к Агенту 2 подтверждён живым тестом в
   Слайсе 7 (`9. Задачник.md` этого проекта: «Docker Desktop + живые контейнеры Агента 2
   подняты... подтверждены ОБА сценария»). Единственное, что подтверждено **не было** — плановый
   опрос `agent3_handoff_queue` через `pollQueues` (см. критичный пункт выше) — стоит сформулировать
   именно так, а не «Агент 3 не задеплоен».

2. **`parsing_results` не хранит метаданные стоимости/инструментов — теперь известна точная
   причина.** У Агента 1 (`intelligence_agent.search_results.meta`) есть `tools_used`/`cost_usd`/
   `duration_sec`/`model_used` — Агент 3 уже умеет это использовать (например, для `reach_estimate`
   из YouTube-метрик, Слайс 8). У Агента 2 такие метаданные **реально считаются** на уровне каждого
   модуля (`modules/*/index.js`) и собираются в `finalResult.meta` в `router/index.js`, но
   сохраняются **только эфемерно**: в Redis-кэш (`setCached`, TTL 1 год) и в Redis-буфер
   `handoff:agent3:{job_id}` (удаляется после доставки) — в саму таблицу `parsing_results` эти поля
   никогда не пишутся (`router/index.js`, INSERT содержит только `job_id, module, result_json,
   confidence_level, confidence_text`). Отдельная таблица `deep_parsing_agent.costs`
   (`id, job_id, tool_id, cost_usd, created_at`), заведённая под это в `8. Архитектура
   (Бекенд).md`, существует только как пустая схема — в неё нигде в коде не пишут (проверено
   grep'ом по `Code/src` на `costs`/`cost_usd`, только вычисления есть, INSERT — нет; подтверждено
   и напрямую: в живой БД `deep_parsing_agent.costs` — 0 строк). Итог: если Агенту 3 или будущему
   Агенту 4 понадобится реальная стоимость/инструменты по конкретному parsing-job'у постфактум —
   этих данных просто не существует нигде постоянно, только в момент самого хендоффа (который
   Агент 3 сейчас не сохраняет, забирает только `result_json`/`confidence`). Не блокирует текущую
   работу, но при желании закрыть — нужно либо писать `meta`/`costs` в Supabase на этапе INSERT в
   `parsing_results`, либо смириться, что это невосстановимо задним числом.

3. **Push-хендофф Агент2→Агент3 по дизайну всегда проваливается, и это нормально — не пытайтесь
   «чинить».** `router.js` пытается напрямую вызвать `AGENT3_MCP_URL/analysis_ingest` при каждой
   успешной задаче, и только при сбое падает в буфер (`bufferForAgent3` → `agent3_handoff_queue`).
   На момент разработки Агента 2 это всегда проваливалось честно, потому что Агента 3 вообще не
   было в сети. **Теперь Агент 3 задеплоен (порт 7302)**, но у него нет и не будет инструмента
   `analysis_ingest` — Агент 3 спроектирован как pull-модель (сам опрашивает
   `agent3_handoff_queue` через `pollQueues`), а не push-приёмник. Значит этот прямой вызов
   по-прежнему будет проваливаться (теперь быстрым отказом от реального сервера на 7302, а не
   таймаутом в пустоту) и falls through в буфер — это ожидаемое поведение, а не баг, который нужно
   чинить добавлением `analysis_ingest` на стороне Агента 3. Просто фиксирую здесь, чтобы никто не
   потратил время на «почему push не работает, хотя Агент 3 уже онлайн».

4. **Схема данных — поправка.** При фиксе критичного пункта выше вскрылась ещё одна неточность
   (уже исправлена в Агенте 3, здесь — для сведения на будущее): `agent3_handoff_queue` реально
   состоит из `id, job_id, result_ref, attempt_count, last_attempt_at, status` — **колонки
   `created_at` в ней нет** (`agent2Reader.js` ошибочно её запрашивал). Метка времени берётся из
   `parsing_jobs.created_at`, которая там реально есть. Остальная структура —
   `parsing_results` (`job_id, module, result_json, confidence_level, confidence_text`),
   `parsing_jobs` (`content_type, content_ref, created_at`) — совпадает с ожиданиями
   `agent2Reader.js`. Дорабатывать здесь нечего, это уже поправлено на стороне Агента 3.

## Раздел C — Общее / инфраструктура

1. **Мастер `docker-compose.yml`** (`Инфраструктура (Docker)/docker-compose.yml`, отдельный
   родительский репозиторий) уже подключает Агента 3 через `include:` (строка с путём к
   `Information analysis agent/Code/docker-compose.yml` раскомментирована и реально работает
   локально) — но не закоммичена, осознанно, по решению пользователя (см. пояснение в чате от
   2026-07-09). Если Агент 1/2 тоже будут коммитить изменения в этот файл — учтите, что он сейчас
   вообще не отслеживается git (`git status` показывает его как `??`, не как «изменён»).

2. **RLS** — см. пункт выше, уже исправлено, касалось таблиц всех трёх агентов разом.

3. **Реальность деплоя: ни один из трёх агентов не на постоянном сервере.** По `09. Деплой.md`
   Агента 1 — весь чек-лист VPS не отмечен (`[ ]`), IP-адрес сервера даже не выбран. По
   `12. Деплой.md` Агента 2 — тоже только локальный Docker Desktop, публичный VPS помечен как
   v1.5. Агент 3 — то же самое. **Фактическая «синхронная работа всех трёх агентов» сегодня — это
   исключительно три контейнера в одном локальном Docker Desktop на машине пользователя**,
   объединённые сетью `marketing-agency-net` (подтверждено живым `docker ps` 2026-07-09: все три
   контейнера + общий Redis подняты одновременно и видят друг друга по имени сервиса). Если
   компьютер выключен — не работает ничего, ни один агент не автономен. Стоит явно держать это в
   голове при планировании: все три «Деплой»-документа рисуют VPS-будущее, которого пока нет ни у
   одного агента.

---

## Тестовое покрытие и реальная готовность — по каждому агенту честно

**Агент 3 (этот проект) — проверен лучше всех.** 294 автотеста (unit + integration через
фейковый Supabase-клиент), плюс многократные живые сквозные прогоны против настоящей прод-БД,
реального Docker, реального Telegram, реального OpenRouter/Gemini. Единственный агент с
полноценным автотест-сьютом. MVP по ТЗ полностью закрыт.

**Агент 2 — тестирование в целом закрыто.** По их `11. Тестирование.md`: unit, integration
(роутер+кэш+очередь), end-to-end через MCP, тесты отказоустойчивости — всё отмечено выполненным.
Только нагрузочное тестирование отложено на v1.5. Живой Docker-прогон всех 5 модулей парсинга
подтверждён. MVP по их собственному ТЗ закрыт — но связь именно с Агентом 3 реально заработала
только 2026-07-09, после фикса cross-schema бага выше.

**Агент 1 — самый непроверенный, и не готов даже по собственному Задачнику.** Почти нет
автотестов (проверено — ни одного `*.test.js` в проекте), основная проверка ручная, и несколько
пунктов в их `06. Задачник.md` до сих пор помечены `[→]` (в работе, не завершено): «Протестировать
все команды бота вручную», «Протестировать оркестратор на реальных запросах», «Тест на реальных
поисковых запросах», плюс всё тестирование записи в `search_results` (4 пункта). Ключевая
недоделка не косметическая: логика «этот контент сложный → передать Агенту 2» физически не
реализована (раздел A, пункт 1 выше) — это не пробел в тестах, а отсутствующая функциональность.

## Сложность оставшихся пунктов — что быстро, что нет

**Действительно быстро (минуты-час), уже сделано в этой сессии:**
- ✅ `mode_content` не сохранял состояние — фикс, коммит `42fdc28`.
- ✅ Уведомления Агент2→Агент1 не доходили до пользователя — фикс, тот же коммит.

**Быстро, ещё не сделано:**
- LangSmith/Helicone для Агента 1 — готовая рабочая конфигурация уже есть у Агента 3, просто
  скопировать (Раздел A, пункт 3).
- Ручное тестирование записи в `search_results` — путь чтения теперь открыт, можно просто прогнать
  (Раздел A, пункт 2).
- Обновление устаревшей строки в `10. Задачник.md` Агента 2 — похоже, уже сделано кем-то отдельно
  от этой сессии (см. коммит `1be51b5` в их репозитории).

**Средний размер (доработка на несколько часов):**
- `meta`/`cost_usd` Агента 2 нигде не сохраняются постоянно (Раздел B, пункт 2) — нужно расширить
  запись в `parsing_results` (или отдельную таблицу `costs`) плюс миграция. Не сложно технически,
  но не однострочник.

**✅ Сделано (2026-07-09):**
- Маршрутизация Агента 1 → Агент 2 (Раздел A, пункт 1) — реализована через `agent2Queue.js`,
  коммиты `13392da` + `503baff`.
- VPS-деплой на постоянный сервер (все три агента, Раздел C, пункт 3) — отдельный крупный этап, не
  баг: регистрация/настройка сервера, домены, автозапуск контейнеров. Все три «Деплой»-документа
  сами относят это к v1.5.

**Чинить не нужно вообще:**
- Push-хендофф Агента 2 на несуществующий `analysis_ingest` (Раздел B, пункт 3) — ожидаемое
  поведение по дизайну.

---

## Как этим пользоваться

Раздел A и B — конкретные задачи, которые логично взять в работу в соответствующих проектах
(`Intelligence agent`, `Deep parsing agent`) в их собственных Задачниках, а не здесь. Пункт про
cross-schema чтение, который блокировал реальный сквозной прогон конвейера, уже исправлен на
стороне Агента 3 (коммиты `304577c`/`b5126f5`, 294/294 теста проходят, живая проверка против
прода подтверждена) — можно смело начинать совместное интеграционное тестирование с Агентом 1/2,
эта преграда больше не мешает. Прочитайте раздел «Сложность оставшихся пунктов» выше, чтобы понять,
с чего начать: быстрые фиксы можно делать сразу, а маршрутизацию Агента 1→2 и VPS-деплой лучше
планировать как отдельные задачи, не пытаться закрыть между делом.
